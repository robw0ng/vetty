# Keep the published .vsix lean — exclude everything not needed at runtime.
.vscode/**
.gitignore
.git/**
**/*.map
scratchpad/**
test/**
dist/**
.github/**
*.vsix
release.bat
node_modules/**
jetbrains/**
docs/**
